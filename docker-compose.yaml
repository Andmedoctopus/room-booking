version: "3.8"

services:
  db:
    image: postgres:13.3
    volumes:
      - ./etc/db/entypoints:/docker-entrypoint-initdb.d
      - pgdata:/var/lib/postgresql/data
    env_file: 
      - ./etc/db/env/dev.env
    ports:
      - 5432:5432

volumes:
  pgdata:
